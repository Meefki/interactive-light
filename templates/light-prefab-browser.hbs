<section class="light-prefab-browser">

  {{!-- ========================= --}}
  {{!-- Панель фильтров --}}
  {{!-- ========================= --}}
  <header class="filters">

    <input
      type="search"
      name="search"
      placeholder="Поиск по названию..."
      value="{{search}}"
      data-action="search"
    />

    <div class="tags">
      {{#each allTags}}
        <label class="tag">
          <input
            type="checkbox"
            value="{{this}}"
            {{#if (includes ../activeTags this)}}checked{{/if}}
            data-action="toggleTag"
          />
          {{this}}
        </label>
      {{/each}}
    </div>

  </header>

  {{!-- ========================= --}}
  {{!-- Список префабов --}}
  {{!-- ========================= --}}
  <ol class="prefab-list">

    {{#each prefabs}}
      <li
        class="prefab {{#if (eq ../activePrefab id)}}active{{/if}}"
        data-id="{{id}}"
        data-action="selectPrefab"
      >

        {{!-- Иконка --}}
        <div class="icon">
          <i class="fa-regular fa-lightbulb"></i>
        </div>

        {{!-- Основная информация --}}
        <div class="info">
          <h4 class="name">{{name}}</h4>

          {{#if tags}}
            <div class="tags">
              {{#each tags}}
                <span class="tag">{{this}}</span>
              {{/each}}
            </div>
          {{/if}}
        </div>

        {{!-- Кнопки управления --}}
        <div class="actions">
          <button
            type="button"
            class="small"
            data-action="editPrefab"
            title="Редактировать (заглушка)"
          >
            <i class="fa-solid fa-pen"></i>
          </button>

          <button
            type="button"
            class="small danger"
            data-action="deletePrefab"
            title="Удалить (заглушка)"
          >
            <i class="fa-solid fa-trash"></i>
          </button>
        </div>

      </li>
    {{else}}
      <li class="empty">
        <p>Префабы не найдены</p>
      </li>
    {{/each}}

  </ol>

  {{!-- ========================= --}}
  {{!-- Подвал --}}
  {{!-- ========================= --}}
  <footer class="footer">

    {{#if activePrefab}}
      <p class="hint">
        🔦 Режим расстановки активен.  
        Клик по сцене — создать источник света.
      </p>
    {{else}}
      <p class="hint muted">
        Выберите префаб для начала расстановки
      </p>
    {{/if}}

  </footer>

</section>
