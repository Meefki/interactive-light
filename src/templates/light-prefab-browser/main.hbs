<section class="prefab-browser">
    <section class="header">
        <input type="search" class="search" placeholder="Поиск префаба..." value="{{search}}" data-action="search" />

        <div class="tag-filters">
            {{#each allTags}}
            <button type="button" class="tag-filter {{#if (includes ../activeTags this)}}active{{/if}}"
                data-action="toggleTag" data-tag="{{this}}">
                {{this}}
            </button>
            {{/each}}
        </div>
    </section>

    <section class="standard-form prefab-container-form scrollable">
        <fieldset class="prefab-container scrollable">
            {{#if prefabs}}
            <div class="grid grid-3col prefab-grid">

                {{#each prefabs}}
                <fieldset class="prefab-card">
                    <input class="input-checkbox" type="radio" name="prefab" id="{{id}}" data-id="{{id}}"
                        data-action="selectPrefab" />
                    <label for="{{id}}" class="prefab-label">
                        <div class="preview">
                            {{#if preview}}
                            <img src="{{preview}}" alt="{{name}}" />
                            {{else}}
                            <i class="fa-solid fa-lightbulb"></i>
                            {{/if}}
                        </div>

                        <div class="name">
                            <span>{{name}}</span>
                        </div>

                        {{#if tags}}
                        <div class="tags">
                            {{#each tags}}
                            <span class="tag">{{this}}</span>
                            {{/each}}
                        </div>
                        {{/if}}
                    </label>
                </fieldset>
                {{/each}}
            </div>
            {{else}}
            <div>
                <h2>Префабы не найдены</h2>
            </div>
            {{/if}}
        </fieldset>
    </section>

    <section class="footer">
        <button>Delete</button>
    </section>
</section>